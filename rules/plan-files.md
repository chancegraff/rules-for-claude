# Plan Files

Every review round or new task gets a NEW, distinctly named plan file in `~/.work/plans/`; never reuse or overwrite a prior round's plan file. If the approval dialog shows an old file, do not overwrite that file and do not call ExitPlanMode; get approval in prose on the new file's content.
