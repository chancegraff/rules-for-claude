# Synthesize Agent Findings

The lead synthesizes teammate findings into one consolidated report; never dump raw per-agent messages and make the user stitch them together (they already see agent messages in the UI; the synthesis is the value added). Synthesize category-by-category with file:line cites, distinguishing residuals from structural issues, ranked by severity, folded into a single actionable summary with a clear verdict.
