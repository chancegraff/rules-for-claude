# Audit Before Planning

When told an implementation "doesn't match the designs", render/read the actual build and diff it against the actual design component-by-component (chrome, styling tokens, behavior, functionality) BEFORE writing any plan; defect lists are discoverable by inspection. Plans are fully complete end-to-end: no "audit during execution", no remaining searching, and reuse-vs-build-new decided per component in the plan.
